# ── Environment ──────────────────────────────────────────────────────────────
# Set to "development" to bypass Firebase token verification (local dev only!)
# NEVER set this to "development" in production.
APP_ENV=production

# Set to "true" to skip auth entirely (dev-only shortcut, implies APP_ENV=development)
# SKIP_AUTH=false

# ── Firebase Admin SDK credentials ───────────────────────────────────────────
# Option A (Railway / CI): paste the full serviceAccountKey.json as single-line JSON
FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Option B (local dev): path to the serviceAccountKey.json file
FIREBASE_KEY_PATH=./serviceAccountKey.json

# Firebase Storage bucket (without gs:// prefix)
FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# ── Google Vision API ─────────────────────────────────────────────────────────
GOOGLE_VISION_API_KEY=your_google_vision_api_key

# ── CORS extra origins ────────────────────────────────────────────────────────
# Comma-separated list of additional allowed origins (e.g. production domain)
# ALLOWED_ORIGINS=https://app.handscript.co.il

# ── Firebase Web API key (auth REST proxy) ────────────────────────────────────
FIREBASE_WEB_API_KEY=your_firebase_web_api_key

# ── Custom transactional email (Brevo HTTP API) ───────────────────────────────
# Sends the designed verification / password-reset emails via Brevo's HTTP API
# (HTTPS) — required because Railway blocks outbound SMTP ports. Leave
# BREVO_API_KEY empty to fall back to Firebase's default email.
# Setup: create a free Brevo account, verify the sender under Senders & IP →
# Senders, then create an API key under SMTP & API → API Keys. Put the real key
# in your .env / Railway variables — NEVER in this committed example file.
BREVO_API_KEY=
BREVO_SENDER_EMAIL=handscript3@gmail.com   # must be a VERIFIED Brevo sender
EMAIL_FROM_NAME=HandScript
