# תוכנית-אב: ארכיטקטורת עריכה→תצוגה→שמירה חדשה

> נוצר 2026-06-12. מטרה: **תאימות מוחלטת** בין מה שהמשתמש רואה למה שהוא מקבל,
> במהירות, בעלות נמוכה, ועמידות בעומס. סטטוס: ממתין לאישור לפני יישום.

---

## 1. אבחון שורש — למה היום זה לא מושלם ולא יכול להיות

| # | בעיית שורש | תוצאה |
|---|---|---|
| 1 | **שני מנועי טיפוגרפיה** — קנבס TS בטלפון (~700 שורות) משכפל את synthesizer.py | כל שינוי בצד אחד שובר את השני. מחלקת באגים אינסופית (ראה SLIDER_ISSUES.md) |
| 2 | **המנוע בשרת לא דטרמיניסטי** — `random.gauss`/`random.shuffle` בלי seed | שני רינדורים של אותו טקסט שונים זה מזה. אי אפשר "לשחזר" את מה שהמשתמש ראה |
| 3 | **הנאמנות תלויה במחזור-בתים** — `/finalize` מעלה את ה-PNG השמור; אם פג → `expired` → רינדור חדש **שונה** | מסלול הכשל שובר את ההבטחה המרכזית של המוצר, בשקט |
| 4 | **אין cache** — כל שינוי סטייל = רינדור PNG מלא ב-300 DPI | איטי (1-3 שניות), יקר ב-CPU וברוחב-פס, לא ישרוד הרבה משתמשים |
| 5 | **אין גרסת מאגר** — שינוי דגימה באמצע סשן משנה את הפלט בלי שליטה | תצוגה ותוצר יכולים להתבסס על מאגרים שונים |

**מסקנה:** אי אפשר "לתקן עוד באג". צריך לשנות את החוזה: רינדור חייב להיות
**פונקציה טהורה** — אותו קלט ⇒ אותם bytes, תמיד.

---

## 2. העיקרון המארגן

```
render(text, style, seed, bank_version, bg, ink, dpi) → bytes זהים, תמיד
```

ומקור אמת טיפוגרפי **אחד**:

```
                 ┌────────────── שרת ──────────────┐
טקסט+סטייל+seed →│ compute_layout()  (טהור, מהיר)  │→ Layout JSON ─→ טלפון: קומפוזיטור
                 │        │                        │   (ציור גליפים במיקומים מדויקים)
                 │        ▼                        │
                 │ rasterize(layout) (PIL, איטי)   │→ PNG/WebP ──→ תצוגה במנוחה + קובץ סופי
                 └─────────────────────────────────┘
```

הטלפון **לא מחשב טיפוגרפיה בכלל**. הוא מקבל מהשרת רשימת "איזו אות, איזה
variant, איפה בדיוק" ומצייר. שבירת שורות, ריווח, ריקוד, נטייה, בחירת variants —
הכל מחושב פעם אחת, במקום אחד, וזהה ב-preview ובקובץ כי זה אותו קוד.

---

## 3. הארכיטקטורה החדשה — רכיב אחר רכיב

### 3.1 דטרמיניזם (backend)

- `seed: int` נוסף ל-`StyleParams`. הטלפון מגריל seed פעם אחת בכניסה לעריכה
  ושומר אותו ב-draft. כפתור "ערבב כתב" 🎲 מגריל seed חדש (פיצ'ר חינמי שנופל מזה).
- כל `random.*` ב-`synthesizer.py`/`layout.py` מוחלף ב-RNG היררכי:
  `rng_glyph = Random(hash(seed, line_idx, word_idx, char_idx))` — כך עריכת טקסט
  בשורה 5 לא משנה את שורות 1–4 (יציבות תחת עריכה).
- `VariantPicker` → חפיסה זרועה לכל (תו, seed). אותו seed = אותן בחירות.
- **bank_version**: שדה מספרי ב-`character_banks/{uid}`, עולה ב-1 על כל
  הוספה/מחיקה/החלפה של דגימה. הרינדור נצמד לגרסה — שינוי מאגר באמצע סשן לא
  משנה תוצר קיים.

**בדיקת קבלה:** `sha256(render(x)) == sha256(render(x))` על 100 קלטים אקראיים.

### 3.2 פיצול המנוע: compute_layout / rasterize (backend)

רפקטור של `synthesizer.py`:

- `compute_layout(text, style, seed, bank_meta) → Layout` — טהור: שבירת שורות,
  מיקומי גליפים, בחירת variants, jitter, tilt, מיקומי כתמים. **בלי PIL, בלי I/O
  של תמונות** (משתמש במטא-דאטה של מידות שנשמר ב-Firestore לצד כל variant —
  נוסף בשמירת דגימה + מיגרציה חד-פעמית לקיימות).
- `rasterize(layout, images, bg, ink, dpi) → pages` — הדבקות PIL בלבד, בלי שום
  החלטה טיפוגרפית.
- `/convert-both` = compute_layout → rasterize. שום לוגיקה לא חיה בשני מקומות.

### 3.3 Endpoint חדש: `POST /layout` (backend)

- קלט: text, style, seed. פלט JSON (קואורדינטות בפיקסלי שרת):

```json
{
  "bank_version": 7,
  "page_geometry": { "w": 2480, "h": 3508, "line_h": 184, "top": 200, "margin": 200 },
  "pages": [{ "lines": [{ "tilt": -3.1, "glyphs": [
    { "ch": "ש", "variant": 2, "url": "...", "x": 2105, "y": 96, "w": 64, "h": 74, "dy": 1.8 }
  ]}]}]
}
```

- אין קידוד תמונה ⇒ מהיר (יעד: p95 < 120ms חישוב). זה מה שמאפשר עדכון חי
  בזמן גרירת סליידר.
- gzip על התשובה (טקסט ארוך = הרבה גליפים; ~30-80KB דחוס לעמוד מלא).

### 3.4 קומפוזיטור (mobile) — מחליף את הקנבס

- קומפוננטה חדשה `LayoutCompositor`: מקבלת Layout JSON + pixelScale, מציירת
  כל גליף כ-`<Image>` ממוקם אבסולוטית. **אפס לוגיקה טיפוגרפית** — רק כפל בקנה
  מידה. הגליפים כבר ב-cache של RN Image (אותם URLs).
- **נמחק מהקליינט:** `CHAR_HEIGHT_RATIO`, `CHAR_ASCENDER_RATIO`, `breakLines`,
  `pickVariantUrl`, `seededRand/seededGauss`, חישובי lsp/wsp/jitter/slant
  (~500 שורות). מחלקת באגי הסנכרון נעלמת מבנית.
- זרימת סליידר: גרירה → throttle ~120ms → `/layout` → קומפוזיטור מתעדכן.
  בשחרור → בקשת תמונה מלאה ברקע; כשמגיעה — החלפה שקטה (הם זהים במיקומים,
  אז ההחלפה כמעט בלתי נראית).
- מצב מנוחה ו-FinalView: **תמיד תמונת השרת** (אמת-קרקע פיקסלית).

### 3.5 רינדור מדורג + cache (backend)

- **Preview**: 150 DPI, WebP (איכות 80) — רבע מהפיקסלים, ~פי 10 פחות בייטים,
  פי 3-4 מהיר יותר לרינדור. **Final**: 300 DPI PNG, clean+photo (כמו היום).
- **Cache key**: `sha256(uid, bank_version, text, style_normalized, seed, bg, ink, mode, dpi)`
  (style מנורמל בשרת — עיגול אחיד — כדי ש-41.0001 ו-41 יהיו אותו מפתח).
- אחסון: `renders/{uid}/{hash}/page-{n}.webp` ב-Cloud Storage, TTL 7 ימים
  (lifecycle rule). בדיקת קיום לפני רינדור ⇒ חזרה על אותם ערכים = מיידי וחינם.

### 3.6 שמירה סופית: `/finalize` = קידום, לעולם לא רינדור-מחדש

- קלט: `render_hash` + idempotency. השרת מבצע **GCS server-side copy** מ-cache
  לנתיב קבוע `documents/{uid}/{doc_id}/` ומעלה usage **פעם אחת** (מפתח
  אידמפוטנטיות = hash ⇒ הקשה כפולה/ריטריי לא מחייבים פעמיים).
- אם אובייקט ה-cache פג: רינדור מחדש עם אותם (text, style, seed, bank_version)
  ⇒ **bytes זהים מובטחים** מהדטרמיניזם. מסלול ה-`expired` השבור נמחק מהעולם.

### 3.7 עמידות בעומס

- Semaphore לרינדור: מקס' 2×CPU רינדורים מקבילים לאינסטנס + תור קצר; מעבר לזה
  `429 + Retry-After`, והקליינט מבצע backoff (יש כבר offlineQueue — מתרחב).
- `/layout` זול פי ~20 מרינדור ⇒ העומס של גרירות סליידרים כמעט לא מורגש.
- Coalescing בקליינט נשאר (AbortController, debounce).
- LRU של מאגרי גליפים בזיכרון לפי (uid, bank_version) — קיים, נשמר.
- הורדת לוגי DEBUG של synthesizer בפרודקשן (TODO פתוח מה-README — נסגר כאן).

### 3.8 מסלולי כשל

| כשל | התנהגות |
|---|---|
| `/layout` נכשל | נשאר ה-layout האחרון + פס "אין חיבור, מנסה שוב"; retry עם backoff |
| רינדור preview נכשל | הקומפוזיטור ממשיך לתפקד (מדויק במיקומים); "סיום" חסום עד רינדור תקין |
| `/finalize` נקטע | retry אידמפוטנטי לפי hash — בלי חיוב כפול, בלי קבצים כפולים |
| מאגר השתנה תוך כדי | הרינדור נצמד ל-bank_version; באנר "המאגר עודכן — לרענן?" |

---

## 4. שלבי ביצוע

**שלב 1 — דטרמיניזם (backend)** ✅ **בוצע 2026-06-12**
seed ב-StyleParams (None → השרת מגריל ומחזיר ב-response) · RNG סדרתי זרוע בכל
המנוע (variants, jitter, ריווחים, ריקוד, כתמים, רעש דיו, אפקט צילום פר-עמוד) ·
bank_version ב-Firestore (עולה בכל שינוי מאגר; 0 לקיימים — בלי מיגרציה) · נחשף
ב-/glyphs ו-/convert-both · בדיקות 41-42: hash-identity + אי-דליפה ל-random גלובלי.
*אחורה-תואם מלא: קליינט ישן בלי seed מקבל התנהגות זהה להיום + seed בתשובה.*

**שלב 2 — פיצול המנוע + מטא-דאטת מידות (backend)**
compute_layout/rasterize · שמירת w/h של כל variant ב-Firestore (+מיגרציה
חד-פעמית לדגימות קיימות) · `/layout` endpoint · בדיקת שקילות: rasterize על
layout ≡ הפלט הישן.

**שלב 3 — cache ורינדור מדורג (backend)**
מפתח hash · WebP 150dpi ל-preview · lifecycle TTL · `/finalize` כקידום אידמפוטנטי.

**שלב 4 — קומפוזיטור (mobile)**
`LayoutCompositor` · PreviewScreen נכתב מחדש סביב `/layout` (הסליידרים נשארים
כמו שהם ויזואלית) · מחיקת מנוע הקנבס · FinalView עובר ל-render_hash.

**שלב 5 — עומס ותצפיות**
Semaphore+429 · backoff בקליינט · מדדי Sentry (זמן layout, זמן רינדור, cache
hit-rate) · בדיקת עומס (k6, 100 משתמשים מקבילים) · הורדת DEBUG.

כל שלב נפרס ונבדק בנפרד; שלבים 1-3 אחורה-תואמים (ה-API הישן ממשיך לעבוד עד שלב 4).

---

## 5. קריטריוני הצלחה (מדיד, לא תחושה)

1. **זהות-bytes**: `hash(תצוגה אחרונה) == hash(קובץ סופי)` ב-100% מהמקרים,
   כולל מסלול פקיעת cache. נבדק אוטומטית.
2. **מהירות**: layout p95 < 250ms מקצה-לקצה · preview מלא p95 < 1.5s ·
   finalize p95 < 1s (קידום, לא רינדור).
3. **עלות**: רוחב-פס preview ↓ ~90% (WebP 150dpi) · cache hit > 60% בסשן
   עריכה טיפוסי · אפס רינדורים על no-op.
4. **עומס**: 100 משתמשים מקבילים בלי שגיאות 5xx; degradation חיננית (429+retry).
5. **קוד**: ~500 שורות טיפוגרפיה נמחקות מהקליינט; אפס כפילות לוגיקה.

---

## 6. סיכונים ומענים

- **הקומפוזיטור לא זהה פיקסל-פיקסל לביטמפ** (פילטר scaling, אלפא): מקובל —
  מצב המנוחה הוא תמיד ביטמפ השרת; הקומפוזיטור חי רק בזמן אינטראקציה,
  והמיקומים זהים ⇒ אין "קפיצה".
- **חישוב layout איטי לטקסט של 25K תווים**: למדוד בשלב 2; הנוסחאות פשוטות
  (אין I/O) — צפוי עשרות ms. אופטימיזציה: numpy על מערכי רוחבים.
- **עריכת טקסט משנה שורות במורד הזרם** (sub-seeds לפי מיקום): מקובל ומתועד —
  זהו כתב יד, לא פונט; מה שחשוב הוא שהתצוגה והקובץ זהים.
- **מיגרציית מידות לדגימות קיימות**: סקריפט חד-פעמי, idempotent, רץ לפני שלב 2.

---

## 7. מה לא משתנה

מסכי Onboarding/מאגר/לכידה · שפת העיצוב · ולידציה (`/validate`) · rate limits ·
מבנה הסליידרים ב-UI (אותם 6, אותה סקאלה 0-100) · ספירת usage (פעם אחת, ב-finalize).
