# בעיות בסליידרים — מסך העריכה (PreviewScreen) ↔ שרת

> נוצר 2026-06-12 לאחר השוואה נוסחה-מול-נוסחה: `PreviewScreen.tsx`, `FinalViewScreen.tsx`,
> `EditorScreen.tsx`, `HandwritingCustomizerScreen.tsx` ↔ `synthesizer.py`, `layout.py`, `main.py`.

---

## ✅ סטטוס תיקונים (2026-06-12)

**תוקן:** 1 (charCode), 3 (occurrences גלובלי), 4 (אינדקס שורה גלובלי ל-slant), 5 (סף 0.5),
6 (word_spacing=0 ב-render_handwriting), 7 (sizeJitter לא משפיע על layout), 8 (jitter גאוסי +
דילוג descenders/yod), 10 (clamp −25% בקליינט), 12 (blobs קרובים יותר לשרת: 55% בקצה, גודל
h/10–h/5, אלפא 180-240, מוצמד לגליף), 14-15 (כל ההערות), 16 (אין פליקר על commit ללא שינוי —
כולל flush ב-handleFinish), 17 (בדיקת שוויון), 18 (אין איפוס עמוד על סטייל), 20 (safety 1.5s
ל-isDragging), 21 (תווית ערך מקומית בלי פיגור + אין snap), 22 (accessibilityLabel/Value),
23 (haptic בשחרור), 26-27 (המסך הוסר; הקובץ רוקן לסטאב — **למחוק ידנית** + הוסר מ-App.tsx
ו-navigation/types.ts).

**הוחלט להשאיר (החלטות מוצר):** 24 (כיוון LTR נשאר), 25 (דיפולטים 15/10 נשארים; draft גלובלי נשאר).

**פתוח (מגבלות מודע):** 2 (שרת לא-זרוע — תועד ביושר, נאמנות דרך תמונת השרת), 9 (ריווח גאוסי
בשרת מול קבוע בקנבס — שבירת שורות עדיין קירוב; השרת נשאר ground truth), 11 (מודל tilt:
רוטציה בשרת מול translateY בקנבס; apply_slant סומן UNUSED), 13 חלקי (אין רוטציית גליף בקנבס),
19 (ביצועי קנבס בטקסטים ארוכים — לא טופל).

---

## א. חוסר סנכרון תצוגה (קנבס) ↔ שרת

### באגים ממשיים

1. **`charCode` לא מועבר ל-`pickVariantUrl`** — PreviewScreen:539 קורא
   `pickVariantUrl(glyphMap[ch], occurrence)` בלי הארגומנט השלישי → seed החפיסה זהה
   לכל התווים. כל האותיות עם אותו מספר variants מתחלפות באותו סדר בדיוק — תחלופה
   מכנית ומתואמת במקום גיוון טבעי. הפונקציה תוכננה לקבל charCode והקריאה מתעלמת ממנו.

2. **בחירת variants בקנבס לא יכולה להתאים לשרת בכלל** — `VariantPicker.pick()` בשרת
   משתמש ב-`random.shuffle` לא-זרוע (אקראי אמיתי), בעוד הקליינט דטרמיניסטי (Mulberry32).
   ההערות בקוד ("Replicates the server's shuffled-deck selection... so the preview matches")
   וה-README מטעים: אפשר לשכפל רק את *החוזה הסטטיסטי*, לא את הבחירות עצמן.
   הנאמנות בפועל מגיעה רק מהצגת תמונת השרת.

3. **ספירת occurrences: פר-עמוד בקליינט, פר-מסמך בשרת** — `charOccurrences` מאותחל
   בכל רינדור של `HandwritingCanvas` וסופר רק את `pageLines` של העמוד הנוכחי; השרת
   סופר על כל המסמך. בעמוד 2+ מיקומי המחזור בחפיסה שונים.

4. **Slant: אינדקס שורה מקומי מול גלובלי** — נוסחת ה-seed זהה במכוון
   (`(i * 2654435761) & 0xFFFFFFFF`, כיוון מ-bit 16, variation `0.6+0.8×`), אבל הקליינט
   משתמש ב-`li` (אינדקס בתוך העמוד) והשרת ב-`line_idx` (גלובלי על כל השורות,
   layout.py:419-429) → בעמוד 2+ כיוון ועוצמת ההטיה לא תואמים.

5. **סף slant שונה** — השרת מדלג כש-`slant_px <= 0.5` (סליידר ≤ 1); הקליינט מציג כל
   ערך > 0. בסליידר 1 הקנבס מטה והשרת לא.

6. **שני מסלולי word_spacing סותרים בשרת** — `compose_paragraph` (synthesizer:1443)
   מכבד 0 מפורש (None-sentinel); אבל `render_handwriting` (שורות 1273, 1299) עושה
   `_st.get("word_spacing", None) or 0` ואז `if word_sp_base > 0 else avg_glyph_w * 1.0`
   → ריווח-מילים 0 מפורש הופך לריווח "טבעי" במסלול הזה. סליידר=0 מתנהג שונה בין מסלולים.

7. **breakLines מתעלם מ-sizeJitter** — שבירת השורות בקנבס מחושבת מרוחבים ללא
   ה-jitter (±3%), אבל הציור מחיל אותו → גליפים בקצה שורה יכולים לזלוג מהשוליים.

### הבדלי מודל (קירוב מודע — אבל גורם לקפיצה ויזואלית בין קנבס לתמונת שרת)

8. **Jitter ("ריקוד")**: שרת — גאוסי (σ = jitter_pct% מגובה התו הספציפי) ומדלג לגמרי על
   descenders (`baseline_dance = 0` עבור ך, ן, ף, ץ, ק, g, j...); קליינט — התפלגות
   אחידה (±σ), מוחל על *כל* התווים כולל descenders, ולפי גובה בסיס ולא גובה-תו.

9. **ריווח: גאוסי בשרת, קבוע בקליינט** — השרת מוסיף `random.gauss` על כל רווח
   (σ=20% לאותיות, 15%-25% למילים) → שבירת השורות בקנבס שונה מהרינדור, וגם
   **בין רינדור שרת אחד למשנהו** (שבירת השורות עצמה לא דטרמיניסטית). מספר העמודים
   יכול להשתנות בין שני רינדורים זהים.

10. **Clamp ריווח שלילי** — שרת מגביל ל-−25% מרוחב אות ממוצע; הקליינט לא מגביל כלל.

11. **מודל ה-slant שונה** — שרת: `_tilt_line` על כל תמונת השורה; קליינט: translateY
    ליניארי פר-גליף. התאמת הסימן/כיוון ב-RTL לא מאומתת. בנוסף, `apply_slant`
    (shear ברמת גליף, synthesizer:1049) הוא **dead code** — "נטייה" בפועל מטה שורות,
    לא אותיות; השם מבטיח איטליק שלא קיים.

12. **כתמי דיו** — שרת: 1-2 כתמים, רדיוס `h//10..h//5`, אלפא 180-240, 55% בקצה הקו
    התחתון; קליינט: כתם יחיד, 9% מגובה, opacity 0.45-0.9, מיקום אחיד על כל הרוחב.

13. **עיוות גליף** — שרת: רוטציה ±2°, scale 0.97-1.03, h/v offsets אקראיים פר-גליף;
    קליינט: רק sizeJitter ±3% מ-seed קבוע. אותו מיקום במסמך מקבל תמיד אותו "אקראי"
    → תבנית קבועה שמורגשת בטקסטים חוזרים.

### תיעוד שקרי (מסוכן לתחזוקה)

14. הערות `slider*0.30 - 10` ב-PreviewScreen:88 וב-synthesizer:1286 — הקוד בפועל **−8**.
15. הערות ה-interface (PreviewScreen:56-57): "letter_spacing 0–30 (slider*0.30)",
    "word_spacing 15–100 (15+slider*0.85)" — בפועל −8..22 ו-0..85.

---

## ב. תפקוד הסליידרים עצמם + אותנטיות התוצאה

16. **פליקר אחרי כל גרירה** — ה-effect מאפס `serverPreviewUrls = null` מיידית על כל
    commit → המסך קופץ מ"תצוגה מדויקת" לקנבס המקורב (שנראה אחרת: variants אחרים,
    עובי קו, blur-hack) למשך 600ms debounce + זמן רינדור. קפיצה ויזואלית בכל שחרור אצבע.

17. **commit בלי שינוי** — tap על thumb בלי הזזה מפעיל `onSlidingComplete` → אובייקט
    `hs` חדש (אין בדיקת שוויון) → רינדור שרת מיותר + פליקר + מחיקת התצוגה המדויקת.

18. **איפוס עמוד תוך כדי גרירה** — `useEffect(() => setCurrentPage(0), [liveHs, ...])`
    רץ על כל עדכון throttle (80ms) → אי אפשר לכוונן סליידר בזמן צפייה בעמוד 2+;
    המשתמש נזרק לעמוד 1.

19. **ביצועים בגרירה** — רי-רנדר מלא של הקנבס כל 80ms; כל גליף = 2 קומפוננטות `Image`
    (שכבת base מטושטשת + שכבה חדה) → מאות Images לעמוד מלא. צפוי jank במכשירים חלשים
    ובטקסטים ארוכים. `React.memo` לא עוזר כי ה-props מתחלפים.

20. **`isDragging` עלול להיתקע true** — אם המחווה מבוטלת ו-`onSlidingComplete` לא נורה
    (קורה ב-Android), תמונת השרת לא תוצג שוב עד גרירה נוספת.

21. **חיווי ערך בפיגור** — התווית מציגה `liveHs` (אחרי throttle 80ms) ולא את מיקום
    ה-thumb בפועל; בנוסף `value={hs[key]}` (controlled) מתעדכן רק ב-commit — עדכון
    ה-prop אחרי שחרור עלול לגרום snap קצר ב-`@react-native-community/slider`.

22. **נגישות** — אין `accessibilityLabel`/`accessibilityValue` על אף Slider (בניגוד לכל
    שאר הכפתורים במסך שמטופלים יפה). קורא מסך שומע רק מספר ללא הקשר.

23. **אין haptics בסליידרים** — `impactLight` קיים בכל כפתור במסך אבל לא ב-onSlidingComplete.

24. **RTL** — הסליידרים בכיוון LTR סטנדרטי (ימינה = יותר) בתוך מסך עברית מלא. ייתכן
    שזה בסדר, אבל זו צריכה להיות החלטה מודעת ועקבית.

25. **דיפולטים קשיחים** — `slant: 15, inkBlobs: 10` לא מגיעים מ-initStyle (שלא נושא
    אותם בכלל) → הרינדור הראשון כולל הטיה וכתמים שהמשתמש לא ביקש. בנוסף draft
    (`preview_draft`) גלובלי ודורס את initStyle לכל מסמך חדש.

26. **HandwritingCustomizerScreen מנותק לחלוטין מהמנוע**:
    - שומר ל-`handwriting_style` ב-AsyncStorage — **אף קוד אחר לא קורא את המפתח הזה**.
      EditorScreen משתמש בקבועים (85/4/35/3). "שמור הגדרות" חסר כל השפעה — פיצ'ר מת.
    - טווחים לא תואמים למנוע: charHeight 20-120 (מנוע 40-130), jitter 0-10% (מנוע 0-25),
      wordSpacing 10-120 (מנוע 0-85 אפקטיבי).
    - ה-LivePreview שלו גנרי לגמרי: variant יחיד קבוע לכל מופע, יחס רוחב קבוע 0.7/0.8,
      בלי `CHAR_HEIGHT_RATIO`/baseline, jitter מבוסס `Math.sin`, בלי נרמול עובי קו —
      מציג "כתב יד" שלא דומה לפלט האמיתי. "ראה את השפעתם בזמן אמת" — מטעה.
    - `onValueChange` → `setHs` ישיר בלי throttle — רי-רנדר על כל פריים גרירה.

27. **שני טיפוסים בשם `HandwritingStyle` עם סמנטיקה שונה** — Customizer מייצא יחידות
    backend-px (20-100), Preview מגדיר 0-100 slider units. אותו שם, אותם שדות, יחידות
    שונות — מתכון לבאג המרה שקט בייבוא הלא נכון.

---

## סדר עדיפויות מוצע

| דחיפות | סעיפים |
|---|---|
| באג מורגש למשתמש | 1, 16, 17, 18, 6 |
| נאמנות קנבס↔שרת | 3, 4, 5, 7, 8, 9, 10 |
| UX/ביצועים | 19, 20, 21, 22, 23, 25 |
| ניקיון/החלטות מוצר | 2 (תיעוד), 11-15, 24, 26, 27 |
